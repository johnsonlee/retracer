version: "3.0"

services:
  app:
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      - SPRING_PROFILE=prod
    ports:
      - "8080:8080"
    volumes:
      - "./data:/data"
